# Music A&R Data Product — dependencias de RUNTIME
# (app de Streamlit + extractores; esto es lo que instala Render / la nube)
# Para desarrollo (dbt, seeds, tests): ver requirements-dev.txt y setup.sh

streamlit>=1.36.0
pandas>=2.0.0
numpy>=1.24.0
duckdb>=1.0.0
requests>=2.31.0
Pillow>=10.0.0

# Extractores de datos reales
spotipy>=2.24.0
python-dotenv>=1.0.0

# ML en producción (carga del modelo serializado desde el notebook)
scikit-learn>=1.4.0
joblib>=1.3.0

# Forecasting de series de tiempo (Módulo 5): Prophet; el módulo hace
# fallback transparente a sklearn si el entorno no puede instalarlo
prophet>=1.1

# Touring geoespacial (Módulo 6): mapa interactivo de la gira simulada
folium>=0.19
streamlit-folium>=0.24
