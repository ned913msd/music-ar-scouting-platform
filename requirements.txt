# Music A&R Data Product — entorno de desarrollo (dbt + DuckDB + análisis)
# Instalar con: bash setup.sh   (crea el venv e instala todo)
#
# ⚠️ Requiere Python 3.10–3.12: dbt aún no soporta 3.13/3.14.
#    setup.sh selecciona automáticamente una versión compatible.

dbt-core>=1.7.0
dbt-duckdb>=1.7.0
duckdb>=1.0.0
pandas>=2.0.0
numpy>=1.24.0
streamlit>=1.36.0
requests>=2.31.0
spotipy>=2.24.0
python-dotenv>=1.0.0
